package alla.verkhohliadova.kcal.repository;

import alla.verkhohliadova.kcal.entity.Groats;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GroatsRepository extends JpaRepository<Groats, Long> {
}
